package io.github.sibmaks.jjtemplate.compiler.benchmark;

/**
 * Selects whether range data is supplied at render time or embedded in the template.
 */
public enum CollectionDataLocation {
    EXTERNAL,
    INLINE
}
