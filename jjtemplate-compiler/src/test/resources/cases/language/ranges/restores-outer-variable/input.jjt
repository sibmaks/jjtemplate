{
  "definitions": [
    {
      "item": "outer",
      "items": ["first", "second"]
    }
  ],
  "template": {
    "{{ result range item,index of .items }}": "{{ .item }}",
    "after": "{{ .item }}"
  }
}
