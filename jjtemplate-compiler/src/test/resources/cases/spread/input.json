{
  "definitions": [
    {
      "defTrue": true,
      "defFalse": false
    }
  ],
  "template": {
    "array": [
      "prefix",
      "{{. .varList}}",
      "suffix"
    ],
    "object": {
      "key": "value",
      "{{. .varObject }}": true
    },
    "staticArray": [
      "{{. list:new 'a', true, 42, 3.1415 }}"
    ],
    "nullSpread": [
      "{{. .notExistsVar }}"
    ],
    "funcOperator": [
      "{{. cast:str .defTrue }}",
      "{{. cast:str .defFalse }}"
    ],
    "funcStaticOperator": [
      "{{. cast:str true  }}",
      "{{. cast:str false }}"
    ],
    "pipeOperator": [
      "{{. .defTrue | cast:str }}",
      "{{. .defFalse | cast:str }}"
    ],
    "pipeStaticOperator": [
      "{{. true | cast:str }}",
      "{{. false | cast:str }}"
    ],
    "ternaryOperator": [
      "{{. .defTrue ? .varParentObject.child : null }}",
      "{{. .defFalse ? null : .varParentObject.child }}"
    ],
    "ternaryStaticOperator": [
      "{{. (false | not) ? .varParentObject.child : null }}",
      "{{. (true | not) ? null : .varParentObject.child }}"
    ]
  }
}