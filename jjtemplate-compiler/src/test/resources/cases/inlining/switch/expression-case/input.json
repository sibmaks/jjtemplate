{
  "definitions": [
    {
      "booleanVar": false,
      "ok": "ok"
    },
    {
      "{{ defResult switch .booleanVar }}": {
        "{{ true }}": "fail",
        "{{ .booleanVar }}": "{{ .ok }}"
      }
    }
  ],
  "template": "{{ .defResult }}"
}