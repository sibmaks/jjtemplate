{
  "template": {
    "array": [
      "{{? .booleanTrue }}",
      "{{? .booleanFalse }}",
      "{{? .string }}",
      "{{? .integer }}",
      "{{? .double }}",
      "{{? .nullVar }}"
    ]
  }
}