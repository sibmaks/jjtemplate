{
  "template": {
    "booleanTrue": "{{ .booleanTrue }}",
    "booleanFalse": "{{ .booleanFalse }}",
    "string": "{{ .string }}",
    "integer": "{{ .integer }}",
    "float": "{{ .float }}",
    "null": "{{ .nullVar }}",
    "array": [
      "{{ .booleanTrue }}",
      "{{ .booleanFalse }}",
      "{{ .string }}",
      "{{ .integer }}",
      "{{ .float }}",
      "{{ .nullVar }}"
    ],
    "fun": "{{ (neg .integer) | str | concat (.booleanTrue | str)}}",
    "nullDot": "{{ .notExisted.child }}"
  }
}